/*const { app } = require('./app')
app.listen(3000, () => {
    console.log('Server is running on port 3000');
})
//npm run test:integration*/

const { app } = require('./app')
app.listen(3000, () => {
    console.log('Server is running on port 3000');
})

